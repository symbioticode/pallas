# PALLAS-M27 — Campagne d'observation 72 h (supervisée, dry-run strict)

Date : lancement le **2026-09-13 à 00:00 EDT** (UTC-4), durée cible **72 h**.
Statut prévisionnel initial : **F-11 (endurance/accumulation) OUVERT** — l'objectif
de cette campagne est de le **clore par l'observation** d'un run réel en conditions
continues, supervisé et signé.

## Contexte — audit v0.5

- `docs/AUDIT-PALLAS-v0.5.md` : 301/301 tests, couverture 86.56/79.59/93.14/86.56,
  scénarios audits 106/106, cargo 65/65. Verdict : NO-GO capital réel, **GO campagne
  court-terme supervisée**. F-11 (démo 12 min : 93/93 cycles `no_signal`, état 0 octet,
  ledger en mode dev non signé) **reste OUVERT**.
- Cause racine du `no_signal` établie : la démo observait un token de marché **expiré
  (2021)** sans carnet. Les `/markets?active=true&closed=false` du CLOB ignorent les
  filtres — il faut utiliser **Gamma API** (`bestAsk`) pour choisir un token vivant.

## Paramètres de la campagne

| Paramètre | Valeur |
|---|---|
| Durée | `PALLAS_CAMPAIGN_MINUTES=4320` (72 h) |
| Intervalle cycle | `PALLAS_CAMPAIGN_INTERVAL_MS=30000` |
| Mesure superviseur | `PALLAS_CAMPAIGN_MONITOR_MS=30000` |
| Incident contrôlé | `PALLAS_CAMPAIGN_INCIDENT=1` → **SIGKILL à ~40 %** (≈ t+28 h 48 min) puis relance |
| Mode ledger | `PALLAS_LEDGER_MODE=supervised` + `PALLAS_LEDGER_PUB_KEY` (chemin) |
| Verification dry-run | faite par `observation-campaign.mjs` AVANT lancement (fatal si inactif) |
| Seuil stratégie | `best ask < 0.6` (défaut), size 1 part |
| Départ | minuit local (2026-09-13 00:00 EDT), géré par `PALLAS_START_AT=midnight` |

## Tokens observés (YES, marchés actifs à carnet réel)

Choisis dans la bande ~1/3–0.6 pour que chaque cycle produise un signal **validé**
(validation `INPUT_VALIDATION` du risk-engine + Kelly positif), exerçant le chemin
décision → état durable → exécution (dry-run).

| Marché | Token CLOB (YES) | bestAsk @probe |
|---|---|---|
| Lula 2026 Brésil | `30630994248667897740988010928640156931882346081873066002335460180076741328029` | 0.47 |
| Flávio Bolsonaro 2026 Brésil | `109876868437950584369987384406356259939519193117253465815665152916226511121427` | 0.502 |
| D contrôle Sénat 2026 | `113287701564209339913693347405685749986285999146352375265161592243948562084773` | 0.54 |
| J.D. Vance 2028 RNC | `40081275558852222228080198821361202017557872256707631666334039001378518619916` | 0.511 |

Note : un seul token signale par cycle (le premier sous seuil) ; la multi-token donne
de la redondance si un marché sort de bande pendant les 72 h.

## Intégrité : contrat de signature opérateur (toutes les 6 h)

- Clés **hors process** dans `~/.pallas-signing/` (`ledger-signing.pem` 0600,
  `ledger-signing.pub.pem` 0644). Le process supervisé ne connaît que la **publique**.
- Au démarrage (t0) : seed de 1 entrée (`campaign_seed`) puis checkpoint initial signé,
  **re-vérifié** par un chargement type supervise avant lancement du superviseur.
- Toutes les **6 h** : re-signature de la tête de chaîne + **re-vérification** de chargement ;
  documentation dans
  `docs/observation/M27-72h-2026-09-13/CHECKPOINTS.md`
  et (machine) `<campagne>/checkpoints.jsonl`.
- Au terme (ou SIGTERM) : checkpoint final + marqueur `<campagne>/COMPLETE`.
- Orchestration : `scripts/m27-72h-run.mjs` + `scripts/observation-campaign.mjs`.

## Artefacts attendus (répertoire de campagne `.pallas/campaign-m27-72h-<ts>`)

`manifest.json`, `metrics.jsonl`, `summary.json`, `logs/*.log`,
`.pallas/{ledger.json, ledger.json.sig, risk-state.json, alerts.jsonl}`,
`checkpoints.jsonl`, `CHECKPOINTS.md`, `COMPLETE`.

À terme : le rapport `docs/OBSERVATION-M27-72h-2026-09-13.md` (grow/path/signatures,
reprise après incident, accumulation état).

## État du lancement (2026-09-12 22:34 EDT)

Lanceur `scripts/m27-72h-run.mjs` démarré **en session isolée** (nohup/setsid, PID
`1594107`), log `.pallas/m27-72h-wrapper.log` ; attente jusqu'à **2026-09-13 00:00 EDT**
(04:00:00Z). À minuit : génération/validation des clés (réutilisées depuis
`~/.pallas-signing`), seed du ledger + checkpoint initial signé et vérifié, puis
lancement du superviseur.

Répertoire de campagne cible :
`.pallas/campaign-m27-72h-2026-09-13T02-34-07-853Z` (créé à minuit, seed inclus).
Vérification à minuit : `tail -f .pallas/m27-72h-wrapper.log` → `supervisor_launched`,
puis `docs/observation/M27-72h-2026-09-13/CHECKPOINTS.md` ligne n°1.

## Surveillance

```
tail -f .pallas/campaign-m27-72h-*/metrics.jsonl          # rss, ledger_entries, state_bytes
tail -f .pallas/campaign-m27-72h-*/logs/*.log             # cycles (allowed, execution)
cat   docs/observation/M27-72h-2026-09-13/CHECKPOINTS.md  # signatures 6 h
ps -eo pid,etime,rss,cmd | grep observation-campaign      # superviseur vivant
```

## Limites assumées

- Pas de `PALLAS_REF_PK` (pas de compte L2 réel) → le scope de réconciliation n'est
  **pas** exercé ; `reconcileAtStartup` (R-01, audit v0.5) reste du code mort.
- Exécution bloquée au niveau `placeOrder` (dry-run strict) — aucune émission réelle.
- Prix illustratifs de la stratégie de référence (PALLAS-M18) : aucun edge prédictif.
- Découverte pendant les warm-up : un ordre de notional sub-centime (prix × size) est
  arrondi à $0.00 → rejet `INPUT_VALIDATION` ; d'où le choix de tokens de bande moyenne.