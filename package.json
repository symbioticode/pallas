{
  "name": "pallas",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc --build packages/core packages/risk packages/execution packages/ledger packages/strategy packages/observatory",
    "typecheck": "tsc --build --dry packages/core packages/risk packages/execution packages/ledger packages/strategy packages/observatory",
    "pretest": "tsc --build packages/core packages/risk packages/execution packages/ledger packages/strategy packages/observatory",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "reference-loop": "node packages/strategy/dist/run-reference-loop.js",
    "observatory": "node packages/observatory/dist/server.js",
    "observatory:demo": "node scripts/observatory-demo.mjs",
    "dry-run": "node scripts/dry-run.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  }
}
