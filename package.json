{
  "name": "pallas",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc --build packages/core packages/risk packages/execution packages/ledger packages/strategy",
    "typecheck": "tsc --build --dry packages/core packages/risk packages/execution packages/ledger packages/strategy",
    "pretest": "tsc --build packages/core packages/risk packages/execution packages/ledger packages/strategy",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "reference-loop": "node packages/strategy/dist/run-reference-loop.js",
    "dry-run": "node scripts/dry-run.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  }
}
